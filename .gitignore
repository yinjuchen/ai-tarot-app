# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# .gitignore
.env

# Private dev notes — not meant to be public
PROJECT_NOTES.md

# Raw screenshot dropped at repo root — the copy used in README lives in docs/screenshot.png
# (leading slash anchors this to the repo root only, so it doesn't also match docs/screenshot.png)
/Screenshot*.png
